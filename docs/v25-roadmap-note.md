# V25 Empire Roadmap

This note documents the V25 product improvement scope. The dashboard now has an empire roadmap layer above the existing command center, strategic advisor and readiness checks.

The build marker file was not changed in this branch because repeated direct writes to `src/buildInfo.ts` were blocked by the tool safety layer during the session.
