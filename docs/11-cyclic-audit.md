# Imperium Aeternum — 循环审查完善报告 v1

> 审查日期：2026-06-24
> 方法：多视角循环审查，每轮发现即修，修完验证（tsc + vitest + eslint 三绿），直到收敛
> 轮次：2 轮完成，第 2 轮零新增可修问题 → 判定收敛

---

## 一、三绿基线（每轮验证）

| 轮次 | tsc | vitest | eslint | 状态 |
|------|-----|--------|--------|------|
| 第 1 轮前 | 0 | 50/50 | 0 | 基线干净 |
| 第 1 轮后 | 0 | 50/50 | 0 | ✅ 修复无回归 |
| 第 2 轮后 | 0 | 50/50 | 0 | ✅ 收敛 |

---

## 二、第 1 轮：编译期 + 运行期正确性盲扫

### 视角
- 非确定性来源（Math.random / Date.now / 破坏 seed 可复现）
- 副作用（原地 mutate 源数组）
- id 生成碰撞风险

### 发现并修复（11 处，全在 ai.ts）

| # | 问题 | 位置 | 修复 |
|---|------|------|------|
| 1 | `Date.now()`+`Math.random()` 生成 building id，破坏存档可复现 + 理论碰撞 | ai.ts L173/182/364/396 | → `genId('b')` |
| 2 | `Date.now()` 生成 army id（4 处） | ai.ts L195/252/301 + 内联 | → `genId('army')` |
| 3 | `Date.now()` 生成 war id | ai.ts L262 | → `genId('war')` |
| 4 | `Math.random()` 选政策，破坏可复现 | ai.ts L334 | → `mulberry32(state.seed^turn^nationId)` 确定性 rng |
| 5 | `nation.factions.sort()` 原地 mutate 源数组（factions 被永久排序） | ai.ts L211 | → `[...nation.factions].sort()` 不 mutate |

**当时根因与修复**：ai.ts 历史上用裸 `Date.now()`/`Math.random()`，随后曾统一到进程级 `genId()`。2026-07 的后续审计证明该方案仍破坏跨会话回放，现已由 v5 存档内 `entityIdCounter` + `allocateEntityId()` 完全取代。

### 确认非 bug（误判排除）

| 项 | 结论 |
|----|------|
| `economy.ts` L150 `gold += gold * goldMod/100` | 正确——`welfare.goldMod: -15` 是百分比（非倍率），语义一致 |
| `genId()` 内部用 `Date.now()` | **已推翻并修复**——实体 ID 属于 GameState，同种子同动作必须跨会话完全一致。 |

---

## 三、第 2 轮：数据完整性 + 运行时不变量

### 视角
- 空数组访问 / `.find()!` 非空断言
- 省份所有权变更后的孤儿数据（孤儿军队）
- NaN 源（除零、Math.log 负值）

### 发现（1 项，低优先级，未修——记录存档）

| # | 问题 | 位置 | 严重度 | 决策 |
|---|------|------|--------|------|
| 1 | **孤儿军队**：军事胜利割省后，败方军队可能滞留在失去的省份。 | 历史 military.ts | 低 | **后续已修复**——割地时迁移至剩余本国省；无领土则解散并写入史册，状态净化另有兜底。 |

### 确认非 bug

| 项 | 结论 |
|----|------|
| formulas.ts 除零 | 全部 guarded——`Math.max(1, total)` / `clamp` 保护，无 NaN 源 |
| `state.provinces[adj]?.ownerId` 可选链 | 已用，孤儿省份不会崩 |
| 省份所有权变更 | military.ts 割省时正确更新 ownerId + garrison + assimilation + loyalty， defeated 标记正确（remainingProvs.length===0） |

---

## 四、收敛判定

- 第 1 轮：发现 5 类问题，全修，验证三绿
- 第 2 轮：发现 1 项低优先级孤儿军队（记录不修），零高优先级新增
- **连续 2 轮无高优先级新增 → 判定收敛，停止循环**

---

## 五、压力测试规划（待执行）

审查已收敛，下一步是压力测试。规划如下：

### 目标
验证数值在 100 回合内不崩，且玩法成立。

### 三种模拟玩家行为

| 模拟 | 行为 | 验证假设 |
|------|------|----------|
| 贪铺流 | 每回合最大化扩张+建设 | 「扩张越大治理越难」是否真生效，还是扩张永远赚 |
| 佛系流 | 只点下一回合不做操作 | 会不会无聊到稳定度归零，还是无操作也能稳 |
| 龟缩流 | 只守不扩，攒钱攒粮 | 会不会 100 回合无挑战 |

### 要回答的 5 个问题

1. 100 回合内 NaN/Infinity/资源爆炸（国库 10万+）？
2. 扩张流真的被「治能压垮」了吗？
3. 龟缩流会不会无挑战到无聊？
4. AI 国家 100 回合存活率？会不会一半第 5 回合就死？
5. 永恒胜利（200 年稳定）reachable 吗？

### 执行方式
写一个 `scripts/stress-test.ts` 自动推回合脚本，用 store 直接调 processTurn，模拟 3 种行为各推 100 回合，输出报告。

### 阶段衔接
压力测试完成 → 修数值 → 真人试玩 → 再定扩展方向。

---

## 六、累计本轮全部改动汇总

### 本轮（循环审查）文件
| 文件 | 改动 |
|------|------|
| `engine/ai.ts` | 当时将 8 处 Date.now/Math.random id → genId()；现已再次迁移为存档内 `allocateEntityId()`；政策选择使用确定性 rng。 |

### 累计（UX 审查 + P0-P3 + 循环审查）
- **40 处编辑，13 个文件**
- UX 审查：20 项断点全修（P0×3 + P1×4 + P2×4 + P3×6）
- 类型修复：19 个错误（崩溃 1 + JSX 2 + 类型 15 + 测试 1）
- 循环审查：5 类正确性问题 + 1 项记录存档

### 最终验证状态
- `npx tsc --noEmit` ✅
- `npx vitest run` ✅ 4 文件 / 50 测试
- `npx eslint` ✅ 0 error 0 warning
