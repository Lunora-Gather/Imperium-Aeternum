# Imperium Aeternum 玩法与逻辑审查记录

## 第一轮已处理

### 1. 状态层稳定性
- 重建 `src/store/gameStore.ts` 为更短、更可维护的安全主状态层。
- 保留页面已有动作接口，避免按钮找不到动作。
- 统一行动点消耗与失败返还，减少“点了没反应但点数被扣”的问题。
- 返回标题页时清理 `pendingScenario`、跳转状态和待选省份，避免菜单残留状态。

### 2. 渲染期副作用
- 修复 `getAmbitionSnapshot()` 在渲染总览页时写入 `state.ambitionMeta` 的问题。
- 现在总览页只读国运目标，只有回合结算才会写入国运进度。

### 3. 构建风险清理
- 移除 `gameStore.ts` 中对不可靠 `recordPlayerAction` 的导入和调用。
- 避免因为不存在的导出导致 TypeScript 构建失败。

### 4. 战争与外交安全
- 玩家宣战使用军事引擎真实签名：`declareWar(state, attackerId, defenderId, targetProvinceId)`。
- 宣战前检查双方是否已处于战争，避免重复战争。
- 议和后双向关系会被同步为停战和负关系。

### 5. 国运目标系统
- 胜利条件根据剧本规模动态缩放。
- 总览页显示征服、富国、合纵、永恒四条路线。
- 避免大地图/大国开局过早胜利。

## 第二轮已处理

### 1. AI 长期战略焦点
- 新增 `src/gameplay/strategyFocus.ts`，补齐入口已经引用但文件缺失的构建风险。
- AI 每回合根据国家状态选择战略：扩张、贸易、修复、防御、研发。
- 战略会持续累积强度，而不是每回合完全随机。
- 玩家周边国家改变战略时，会写入年报“天下大势”。

### 2. 事件选择体验
- `EventModal` 升级到 v4。
- 事件选项现在显示更完整的效果：金、粮、木、铁、人口、稳定、合法、腐败、治能、厌战、影响力、行政点、科研点、税率、外交关系、同化、派系满意度。
- 每个选项增加“后果”说明，提示可能引发的饥荒、叛乱、合法性危机、战争压力、事件链等。

### 3. 年报决策建议
- `TurnReportScreen` 升级到 v3。
- 年报顶部新增“下一年优先级”，按紧急程度给出建议。
- 建议卡可直接跳转到经济、政治、军事、外交、科技等对应页面。

## 第三轮已处理

### 1. 军事页引导化
- `MilitaryScreen` 加入“军情判断”面板。
- 根据是否战争中、是否有前线军队、厌战、兵力规模、弱状态军队自动给出建议。
- 军队卡片会标记“前线”，更容易知道哪支军队能影响战事。
- 宣战候选仍保留战力对比，但复用了统一的候选列表，降低重复逻辑。

### 2. 省份页引导化
- `ProvinceScreen` 升级到 v3，改为“省政判断 → 高风险省份 → 省份列表 → 选中省操作”。
- 自动计算省份风险分数，优先列出最危险省份。
- 给出省政建议：先平叛、保粮、补金，或按国运目标建设。
- 建筑操作保留科技锁、收益预览、升级、拆除、征兵、开垦和驻军调动。

## 第四轮已处理

### 1. 国策与 AI 战略合并
- `strategyFocus.ts` 现在同时负责玩家国策和 AI 战略，避免 `.ts` 遮住旧 `.tsx` 后造成 `StrategyFocusId` 类型缺失或国策按钮失效。
- 保留玩家国策：均衡、安民、富国、强军、睦邻、改革。
- 保留 AI 长期战略：扩张、贸易、修复、防御、研发。
- 新增 `getAIStrategyInfo()`，供外交页读取 AI 意图。

### 2. 外交页引导化
- `DiplomacyScreen` 升级到 v3。
- 顶部新增“外交判断”，直接列出威胁对象、可贸易对象、可结盟对象。
- 各国关系卡显示 AI 意图、目标、战略强度和起始年份。
- 保留列表/图谱切换，图谱节点仍可跳回对应国家卡片。

## 第五轮已处理

### 1. 经济页引导化
- `EconomyScreen` 升级到 v3。
- 顶部新增“经济判断”，优先提示缺粮、赤字、腐败、税率过高、贸易依赖风险。
- 税率区保留快捷按钮与键盘微调，并新增增税/降税收益预估。
- 上回合收支、资源变化、贸易路线、禁运与贸易风险继续保留。

### 2. 政治页引导化
- `PoliticsScreen` 升级到 v3。
- 顶部新增“政局判断”，优先提示内战、稳定危机、合法性危机、腐败、派系不满、行政点不足。
- 新增派系温度卡，直接显示最不满的派系。
- 政策/法律列表继续保留，卡片抽成组件后更容易维护。

## 第六轮已处理

### 1. 科技页路线化
- `TechnologyScreen` 升级到 v3。
- 顶部新增“科技路线判断”，根据粮食、战争、腐败、治能、合法性、影响力自动推荐农业、军事、行政、文化路线。
- 科技按钮现在同时检查科研点和金钱，不再只因金钱判断是否可研发。
- 科技树保留原有可视化，但卡片状态更清楚：已学、研发中、科研不足、金不足、锁定。

### 2. 开局页引导化
- `ScenarioSelect` 升级到 v3。
- 剧本大厅新增“第一次玩 / 大地图 / 贸易 / 高压挑战”的推荐卡。
- 每个剧本显示玩法风格与难度标签。
- 选国页给国家增加风格标签：强国、稳健、挑战、贸易、军事、行政、民生。

## 第七轮大优化已处理

### 1. AI 战略记忆
- `strategyFocus.ts` 新增 AI 记忆层。
- 每个 AI 会长期记录：宿敌、亲近对象、关注对象。
- 宿敌由战争、威胁、低关系、边境军力差共同影响。
- 亲近对象由贸易、同盟、关系、信任共同影响。
- 关注对象由威胁、关系波动、军力差共同影响。
- AI 战略选择会优先参考记忆，而不是每回合只看当前数值。

### 2. 外交页记忆显示
- `DiplomacyScreen` 升级到 v4。
- 各国关系卡新增：宿敌、亲近、关注三类记忆标签。
- 外交判断会把“对方已将玩家视作宿敌”的国家排进最高威胁。
- 贸易推荐会优先考虑已经倾向亲近玩家的国家。

### 3. 人口页引导化
- `PopulationScreen` 升级到 v3。
- 顶部新增“人口判断”，提示民怨、阶层不满、派系风险、地方忠诚。
- 阶层卡会标记低满意风险。
- 派系卡会提示“高权力低满意”的政治风险。

### 4. 统计页大局诊断
- `StatsScreen` 升级到 v2。
- 顶部新增“大局诊断”，根据最近趋势判断财政、粮食、不满、厌战、外部军力压力。
- 统计页不再只是图表，而是先给趋势结论，再给图表验证。

## 第八轮大优化已处理

### 1. AI 领土记忆
- `strategyFocus.ts` 新增 AI territorial memory。
- AI 现在会记录核心省份、觊觎省份、复仇省份与领土压力。
- 觊觎省份由邻接、省份人口、农业基础、建筑、首都价值、敌对关系和军力差共同影响。
- 复仇省份会从战局中提取，战争进度不利时会留下复仇目标。
- AI 是否转向扩张会参考领土压力，不再只看国家性格和军力。

### 2. 外交页领土野心显示
- `DiplomacyScreen` 升级到 v5。
- 各国关系卡新增核心、觊觎、复仇标签。
- 若 AI 觊觎玩家省份，会被排入外交页最高威胁。
- 外交判断文案会直接提示“觊觎你的省份”。

### 3. 史册页长期叙事
- `ChronicleScreen` 升级到 v2。
- 新增“史册判断”，自动判定当前是危机纪元、征伐纪元、改革纪元、富庶纪元或奠基纪元。
- 新增时间线/分类切换。
- 时间线按年份展示帝国历程，近年摘要放在底部。

### 4. 存档页体检
- `SaveLoadScreen` 升级到 v5。
- 新增“存档体检”，显示已用槽位、手动存档、空槽位、旧档数量。
- 自动给出建议：是否需要手动存档、是否有旧档、是否应保留分支路线。
- 槽位卡新增当前国家、自动档、旧档修复等标签。

### 5. 版本标记
- 应用入口标记更新为 `大优化 v8 · territorial-chronicle-save`。

## 仍建议继续优化

### A. 回合引擎深层重构
- `turn.ts` 仍有旧版浅拷贝和部分原地修改逻辑。
- 当前用 `logicGuard.ts` 做安全包装，但长期应逐步把结算迁移为纯函数。

### B. AI 领土行动闭环
- AI 已能记住核心、觊觎和复仇省份，但还未把这些目标强绑定到宣战目标选择和和约收益偏好。
- 下一步建议让 AI 宣战优先选择 desired/revenge 省份，并在年报里明确提示战略目标变化。

### C. 史册事件联动
- 史册页已经能展示叙事，但还可以让更多重大事件自动写入 chronicle，例如长期宿敌形成、领土野心暴露、王朝更替等。

### D. 存档兼容
- 新增字段通过读取兜底，不会要求旧档必须存在。
- 后续新增字段时仍应保持旧档可读，不直接破坏 localStorage。
