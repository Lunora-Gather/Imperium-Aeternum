// Imperium Aeternum — 应用入口（5a 最小骨架）
// 5c 会替换为完整路由 + 11 页面

import { createRoot } from 'react-dom/client';
import App from './App';
import './index.css';

createRoot(document.getElementById('root')!).render(<App />);
