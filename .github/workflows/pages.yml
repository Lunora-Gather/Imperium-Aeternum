name: Deploy GitHub Pages Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled duplicate workflow
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "This duplicate Pages workflow is disabled."
          echo "Use the main-branch workflow: Deploy Imperium Aeternum Preview."
